============================================
 	; Title: Discussion 4.1
 	; Author: Perry Fulfs
 	; Date: 16 June 2020
 	; Modified By: Erica Perry
 	; Description: create an array
 	; that outputs random motorcycle brand name
 	;===========================================
 	*/
 	 
 	// output: random motorcyle brand
 	// FIXME:
 	 
 	function random_item(items)
 	{
 	 
 	return items[Math.floor(Math.random()*items.length)];
 	 // needs paratheses so the function will work properly to output a random number, adding parethes bethween the .random and*
 	}
 	 
 	var items = ["YAMAHA", "HONDA", "TRIUMPH", "HARLEY DAVIDSON", "DUCATI"];
 	console.log(random_item[]);
  // in the consol.log you will see paratheses, I'm moving the paratheses and addimg curly brackets [] because it is an array you wish to call apond
